import React from "react";
import styled from "styled-components";

const ErrorStyled = styled.div``;

function Error() {
  return <ErrorStyled>Ups, parece que ha ocurrido un error... Intenta mas tarde.</ErrorStyled>;
}

export default Error;
